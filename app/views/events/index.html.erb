<h1 class="text-center mt-5">Here is a list of all events happening right now!</h1>
<div class="row">
        <% @events.each do |event| %>
            <div class="column">
                <div class="card m-5" style="width: 18rem;">
                    <div class="card-body">
                        <h3 class="card-title">Name: <%= event.name %></h3><br>
                        <p class="card-text">Date: <%= event.date %></p>
                        <p class="card-text">Location: <%= event.location %></p>
                        <p class="text-center">People Attending: <%= event.event_attendees.count %><p>
                        <%= render "attendee/attendee_form", event: event %>
                        <%= link_to "View Event", event_path(event), method: :get, class: 'btn btn-primary btn-sm' %>
                    </div>
                </div>
            </div>
        <% end %>
</div>
